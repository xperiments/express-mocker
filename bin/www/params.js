var route ='/edit/:category/:id/:demo3';
var paramsRegExp = /(:.[^\/]*)/g;
var paramNames = route.match( paramsRegExp ).map(function(el){ return el.replace(':','') });

var paramObject = {};
paramNames.map(function(el){ paramObject[ el ] ='desc' } );

var route ='/edit/:category/:demo';
var paramsRegExp = /(:.[^\/]*)/g;
var paramNames = route.match( paramsRegExp ).map(function(el){ return el.replace(':','') });

var paramObject2 = {};
paramNames.map(function(el){ paramObject2[ el ] ='desc2' } );




function mergeParams( a,b )
{
	var c = {}
	for( var i in b )
	{
		if( a[i] )
		{
			c[i] = a[i];
		}
		else
		{
			c[i] = b[i];
		}

	}
	return c;
}

console.log( paramObject, paramObject2, mergeParams( paramObject, paramObject2 ) );





<div ng-controller="EntityForm">
	<form class="form-horizontal">
		<div class="control-group" ng-repeat="element in form">
			<label class="control-label">{{element.label}}</label>
			<div class="controls" ng-switch="element.widget">
				<div ng-switch-when="text">
					<input type="text" ng-model="desc" ng-change="changeFn('desc',element.model, desc )" />
					<input type="text" ng-model="def" ng-change="changeFn('def',element.model, def )" />

				</div>
				<select ng-switch-when="select" ng-model="entity[element.model]" ng-required="element.required" ng-options="o.key as o.name for o in element.options"></select>
			</div>
		</div>

		<button class="btn" ng-click="updateEntity()">updateEntity()</button>
	</form> <pre>{{ entity|json }}</pre>

</div>




var app = angular.module('myApp', []);

app.controller('EntityForm', function ($scope) {

	var paramsRegExp = /(:.[^\/]*)/g;
	var route ='/edit/:category/:id/:demo3';
	var paramNames = route.match( paramsRegExp ).map(function(el){ return el.replace(':','') });
	// Data to be edited in the form
	// (would normally by fetched from the server)
	$scope.entity = {};
	$scope.form = {};
	paramNames.map( function( el ){
		console.log( el )
		$scope.form[ el ] = {
			label:el,
			name:el,
			model:el,
			required:true,
			widget:'text'
		}

	});
	$scope.changeFn = function( prop, model, value )
	{
		$scope.entity[model] = $scope.entity[model] || {};
		$scope.entity[model][prop] = value;
	}
	$scope.updateForm2 = function( model, value )
	{
		$scope.entity[model] = value;
	}
	console.log( $scope.form );
	// Object representing the form (will be generated by GAE).
	$scope.form1 = {
		name: {
			label: 'Name',
			model: 'pepe',
			widget: 'text',
			required: true
		}
	};

	$scope.updateEntity = function () {
		$scope.entity.name = "Specialty's Cafe [updated]";
	};
});